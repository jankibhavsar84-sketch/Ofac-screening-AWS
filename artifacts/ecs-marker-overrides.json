{"containerOverrides":[{"name":"backend","command":["python","-c","import sys; print(\"marker: start\", flush=True); from app.config import settings; print(\"marker: config\", flush=True); from app.repository import JobRepository; print(\"marker: repo-imported\", flush=True); repo = JobRepository(settings.app_db_path, settings.app_db_url); print(\"marker: repo-init\", flush=True); from app.queue import SqsQueue; print(\"marker: queue-imported\", flush=True); queue = SqsQueue(); print(\"marker: queue-init\", flush=True); from app.sns_notifier import SnsNotifier; print(\"marker: notifier-imported\", flush=True); notifier = SnsNotifier(); print(\"marker: notifier-init\", flush=True); from app.screening_service import ScreeningService; print(\"marker: service-imported\", flush=True); service = ScreeningService(repository=repo, queue=queue, notifier=notifier); print(\"marker: service-init\", flush=True); from app.actimize import ActimizeClient; print(\"marker: actimize-imported\", flush=True); actimize = ActimizeClient(); print(\"marker: actimize-init\", flush=True); from app.file_store import S3FileStore; print(\"marker: file-store-imported\", flush=True); file_store = S3FileStore(); print(\"marker: file-store-init\", flush=True)"]}]}
